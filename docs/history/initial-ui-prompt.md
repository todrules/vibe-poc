Using the tech stack we defined (Next.js App Router, React 19, TypeScript, Tailwind CSS, Untitled UI React, ESLint with React rules), generate the initial UI scaffolding for the project.

Deliverables:
1. Create the folder structure for the Next.js app.
2. Generate the following files with placeholder content:
   - app/page.tsx (main UI layout)
   - components/RequirementsInput.tsx
   - components/FileUpload.tsx
   - components/GenerateButton.tsx
   - components/OutputSection.tsx (collapsible section using Untitled UI)
3. Configure Tailwind CSS (tailwind.config.ts, globals.css).
4. Add ESLint config for React + TypeScript + Next.js.
5. Add placeholder TypeScript types for:
   - AcceptanceCriteria
   - AgentPrompts
   - UserStory
   - TestCase

Rules:
- Do NOT implement backend API calls yet.
- Use Untitled UI React components for layout, cards, accordions, buttons, and typography.
- Use Tailwind utility classes for spacing and layout.
- Add comments explaining the purpose of each file.

Begin by generating the folder structure and the empty files.
