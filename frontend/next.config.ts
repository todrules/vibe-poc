import type { NextConfig } from "next";

// Keep the initial Next.js config minimal until routing, images, and API boundaries are defined.
const nextConfig: NextConfig = {
	output: "standalone",
};

export default nextConfig;